import { StyleSheet, Text, View } from "react-native";
import React from "react";

const textGenCard = () => {
  return (
    <View>
      <Text>textGenCard</Text>
    </View>
  );
};

export default textGenCard;

const styles = StyleSheet.create({});
